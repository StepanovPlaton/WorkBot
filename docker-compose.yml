services:
  most-bot:
    build: .
    container_name: most-bot
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config/config.yaml:ro
    environment:
      TZ: Asia/Yekaterinburg
